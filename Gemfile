source 'https://rubygems.org'

gem 'jekyll'

group :jekyll_plugins do
    gem 'jekyll-archives-v2'   # year, tag and category archives under /blog/
    gem 'jekyll-feed'          # /feed.xml
    gem 'jekyll-minifier'
    gem 'jekyll-paginate-v2'
    gem 'jekyll-sitemap'
end
